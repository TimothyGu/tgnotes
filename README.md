# A New Easy-to-Type and Handwrite Text Format

This is the Git repository of the TGNotes Text Format. It contains the
reference implementation of the format in `notes2md.sh`, and the specification
in `SPEC.md`.
